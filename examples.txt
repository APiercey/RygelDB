# Create a Collection
DEFINE COLLECTION collection_name

# Store record into
STORE INTO collection_name DATA

FETCH ALL FROM collection_name WHERE data.property.value IS "twenty two"
FETCH 1 FROM collection_name WHERE data.property.value IS "twenty two"
FETCH 23 FROM collection_name WHERE data.property.value IS "twenty two"

REMOVE key IN collection_name
